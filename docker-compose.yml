version: '3.8'

services:
  app:
    build: .
    container_name: electro_fennassa_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - JWT_SECRET=electro_fennassa_secure_jwt_secret_2026_taourirt
    restart: always
